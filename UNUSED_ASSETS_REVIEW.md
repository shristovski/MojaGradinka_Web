# Unused assets review

Per the Production Optimization Brief: nothing gets deleted without an explicit
owner decision. This is a list of files in `assets/` that `index.html`,
`privacy.html`, `terms.html` and `app.css` do **not** currently reference,
generated by grepping every `src=`, `srcset=`, `href=` and CSS `url()` in the
project. Review each row and either delete it, or note what it's being kept
for.

| File | Likely reason unused | Suggested action |
|---|---|---|
| `administracija.png` | Photo card from an earlier (pre-tab-redesign) version of the "Сè" section | Safe to delete once confirmed no future section needs it |
| `app_functionality.png` | Reference screenshot, superseded by the composited phone mockups | Safe to delete (source material, not final art) |
| `badge-floral-cream-full.svg` | Decorative badge asset not used by the current layout | Delete, or keep if planned for a future section |
| `komunikacija.png` | Photo card from an earlier "Сè" section version | Safe to delete once confirmed |
| `leaf_03_blue_teardrop.svg` | Extra decorative leaf variant, not currently placed anywhere | Keep as a spare decoration option, or delete |
| `leaf_05_gold_three_lobed.svg` | Extra decorative leaf variant, not currently placed anywhere | Keep as a spare decoration option, or delete |
| `lockup-forest-old.svg` | Superseded by `lockup-forest.svg` | Safe to delete — confirmed old version |
| `lockup-white-old.svg` | Superseded by `lockup-white.svg` | Safe to delete — confirmed old version |
| `Mobile_App_Odblisku.png` | Raw source screenshot used to composite `phone-mockup-cta.png` | **Keep** — source file for regenerating the mockup if the screenshot changes |
| `mobile_app.jpg` | Early reference screenshot, superseded | Safe to delete once confirmed |
| `Mobilna_App.png` | Raw source screenshot, tried in `.mg-scene-phone` and reverted per your request to keep both mockups matching | **Keep** — source file, same reason as above |
| `naplata.png` | Photo card from an earlier "Сè" section version | Safe to delete once confirmed |
| `phone_14_01.eps` | Original Adobe Illustrator iPhone-14 mockup file used to build `phone-mockup-live.png`/`phone-mockup-cta.png` | **Keep** — source file for regenerating mockups (Ghostscript + Pillow pipeline) |
| `playing_bears.png` | Alternate decorative photo, not currently placed | Safe to delete once confirmed |
| `prisustvo.png` | Photo card from an earlier "Сè" section version | Safe to delete once confirmed |

Not listed above because they're intentionally referenced only via absolute
URL in `<meta property="og:image">`/`twitter:card` (not a relative
`src`/`href`, so the grep above wouldn't catch it): `social-preview.jpg` — in
use, keep.

## Recommendation
The four rows marked **Keep** are raw source material for the phone-mockup
compositing pipeline (documented in the project's working notes) and should
stay even though nothing in the HTML/CSS points at them directly. Everything
else above looks like leftover material from earlier iterations of the "Сè"
section and old logo versions — low risk to delete, but left in place
pending your confirmation since none of it was ever explicitly requested to
be removed.
